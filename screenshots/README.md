# Screenshots

Drop image files here and reference them from the main README. The README currently
expects:

- `tasks.png` — the tasks list / home screen
- `assignment.png` — the 'why you got this task' breakdown
- `calendar.png` — the calendar sync view

Suggested extras worth adding: the household leaderboard, the AI assistant chat, and a
short demo GIF of completing a task with photo proof.

Keep images reasonably sized (under ~1 MB each) so the README loads quickly.

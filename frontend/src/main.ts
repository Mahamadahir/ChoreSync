/** Entry point for the ChoreSync SPA. */

export function bootstrapApplication(): void {
  /**
   * TODO: Mount the chosen frontend framework, hydrate initial state from the backend,
   * and register real-time channels for task updates.
   */
  throw new Error('TODO: implement SPA bootstrap logic');
}

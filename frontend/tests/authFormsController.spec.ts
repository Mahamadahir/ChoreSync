import { AuthFormsController } from "../src/controllers/AuthFormsController";

describe("AuthFormsController", () => {
  test.skip("handleLogin should authenticate the user", async () => {
    throw new Error("TODO: expectations for handleLogin");
  });

  test.skip("handleSignup should create a new account", async () => {
    throw new Error("TODO: expectations for handleSignup");
  });
});

import { getUser, login, logout } from "../src/services/AuthGateway";

describe("AuthGateway", () => {
  test.skip("login should authenticate via API", async () => {
    throw new Error("TODO: expectations for login service");
  });

  test.skip("logout should terminate session via API", async () => {
    throw new Error("TODO: expectations for logout service");
  });

  test.skip("getUser should retrieve current user", async () => {
    throw new Error("TODO: expectations for getUser service");
  });
});

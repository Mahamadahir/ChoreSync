import { bootstrapApplication } from "../src/main";

describe("bootstrapApplication", () => {
  test.skip("should mount the SPA and register real-time handlers", () => {
    throw new Error("TODO: add expectations for bootstrapApplication once implemented");
  });
});

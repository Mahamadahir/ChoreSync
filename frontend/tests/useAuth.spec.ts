import { checkAuth, logout, useAuth } from "../src/composables/useAuth";

describe("useAuth composable", () => {
  test.skip("useAuth should provide authentication helpers", () => {
    throw new Error("TODO: expectations for useAuth");
  });

  test.skip("checkAuth should verify the current session", async () => {
    throw new Error("TODO: expectations for checkAuth");
  });

  test.skip("logout should clear session state", async () => {
    throw new Error("TODO: expectations for composable logout");
  });
});
